# 부하테스트 시뮬레이터 (loadtest-sim)

## 이 프로젝트는 무엇인가

k6 → 로드밸런서 → 앱서버 → DB 구조를 로컬 Docker Compose 위에 만들고,
**단 하나의 라우트**를 계층별로 튜닝해 가며 백엔드 성능/부하를 학습하는 프로젝트다.

**목표 SLO (단일 라우트 기준)**
- 처리량: 1000 RPS 지속
- 지연: p99 < 200ms
- 에러율: < 0.1%

**중요: 이 프로젝트의 산출물은 동작하는 코드가 아니라 "왜 병목이 생겼고 어떻게 풀었는지에 대한 측정 기록"이다.**
코드는 실험 도구일 뿐이다. `docs/labs/` 가 진짜 결과물이다.

---

## 작업 규칙 (반드시 지킬 것)

### 1. 나는 학습 중이다 — 설명 먼저, 승인 후 구현
구현/수정 전에 항상 **"왜 이렇게 하는지"를 3~5줄로 먼저 설명**하고 내 승인을 받는다.
- 설명에는 최소한 이 세 가지가 들어가야 한다: ① 지금 무엇이 병목이라고 보는가 ② 이 변경이 그 병목을 어떻게 건드리는가 ③ 이 변경으로 어떤 지표가 어느 방향으로 움직일 것으로 예상하는가.
- 내가 "구현해줘"라고 명시적으로 말하기 전까지 파일을 쓰지 않는다.
- 예외 없음. "간단한 수정이라서" 는 예외 사유가 아니다.

### 2. 한 번에 한 계층만 건드린다
계층 = k6 / 로드밸런서 / 앱서버 / 캐시 / MQ / DB.
- 한 실험에서 두 계층을 동시에 바꾸지 않는다. 원인 귀속이 불가능해지기 때문이다.
- 한 계층 안에서도 변수는 가능한 한 하나만 바꾼다 (예: 커넥션 풀 크기만, LB 알고리즘만).
- 여러 개를 바꿔야 한다고 판단되면 먼저 그 사실을 말하고 실험을 쪼갠다.

### 3. 모든 실험은 `docs/labs/NN-<주제>.md` 에 기록한다
파일명 예: `docs/labs/03-connection-pool-sizing.md`
아래 6개 섹션을 **반드시 이 순서로** 포함한다.

```markdown
# NN. <주제>

## 가설
(무엇이 병목이라고 생각하는가. 무엇을 바꾸면 무엇이 어떻게 변할 것이라 예상하는가.
 실험 전에 쓴다. 실험 후에 고쳐 쓰지 않는다 — 틀린 가설도 그대로 남긴다.)

## 설정
(변경한 값, 변경하지 않은 값, docker compose 구성, k6 시나리오 파라미터,
 하드웨어/컨테이너 리소스 제한. 다른 사람이 그대로 재현할 수 있을 만큼 적는다.)

## 측정결과
(k6 실제 출력. RPS, p50/p95/p99, 에러율, 실패 유형별 카운트.
 DB/앱 지표도 함께: 커넥션 수, 락 대기, CPU, 메모리.
 원문 요약을 붙여넣는다. 손으로 다시 쓰지 않는다.)

## 병목 원인
(측정결과에서 어떤 근거로 그 결론에 도달했는지. 근거 없는 원인 추정 금지.)

## 개선
(무엇을 바꿨는가. 왜 그 방법을 골랐는가. 고려했지만 버린 대안과 그 이유.)

## 재측정
(개선 후 같은 시나리오로 다시 측정한 결과. 개선 전/후 숫자를 나란히 놓는다.
 개선되지 않았다면 그것도 그대로 기록한다.)
```

### 4. 성능 수치는 추측하지 않는다
- "보통 이 정도 나온다", "일반적으로 30% 개선된다" 같은 말을 하지 않는다.
- 모든 성능 주장은 **실제로 실행한 k6 결과**를 근거로 한다. 근거가 될 실행 결과가 없으면 "아직 측정하지 않았다"고 말한다.
- 일반론(개념 설명)과 이 프로젝트의 측정치를 문장에서 분명히 구분한다.
- 측정하지 않고 최적화를 제안해야 하는 상황이면, 먼저 "이걸 확인할 측정부터 하자"고 제안한다.

### 5. 각 Phase 완료 시 `git tag phase-NN` 을 남긴다
- Phase 완료 조건: 해당 Phase의 lab 문서가 `재측정` 섹션까지 채워졌고, 그 결과를 내가 확인했을 때.
- 태그는 되돌아가서 "그때 그 구성"을 그대로 재현하기 위한 것이다. 태그 시점의 compose 파일과 k6 스크립트가 커밋에 포함되어 있어야 한다.
- 태그 메시지에 해당 Phase에서 도달한 RPS / p99 / 에러율을 적는다.

---

## Phase 로드맵

| Phase | 주제 | 완료 기준 |
|---|---|---|
| 01 | 취약 기준선 + 측정 신뢰성 (k6 부하 모델, 워밍업, 생성기 한계, 앱 단독 상한) | ✅ 천장 2000 RPS / 병목 = 앱 단일 스레드 CPU |
| 02 | HTTPS TLS 핸드쉐이크 부하 (앱 종료 vs 프록시 종료) | ✅ 핸드쉐이크 강제 시 천장 절반 / 프록시 오프로딩으로 회복 |
| 03 | 리버스 프록시 / 로드밸런싱 알고리즘 / 헬스체크 | ✅ least-conn 이 p99 7.4배 개선 / drain 유무가 p99 45배 |
| 04 | 앱서버 물리적 한계 (CPU/메모리/동시성/큐잉) + 과부하 보호 | ✅ 요청당 CPU 가 천장을 정함 / load shedding 이 p50 을 250배 개선 |
| 05 | ORM 커넥션 풀링 + DB 메모리/유휴 커넥션 | ✅ 커넥션당 사적메모리 3.1MB 선형 / 인덱스 하나가 처리량 7.9배 |
| 06 | DB 프록시 (PgBouncer) | ✅ 앱 대수·풀 크기와 무관하게 DB 커넥션 22개 고정 / 메모리 53% 절감 |
| 07 | 로컬 캐시 vs Redis | 두 방식의 히트율/일관성/p99 트레이드오프를 측정했다 |
| 08 | Message Queue 기반 분산처리 | 동기 경로 축소 전후 p99와 큐 lag을 측정했다 |
| 09 | 서버리스 + 클라우드 확장 | 로컬에서 찾은 병목이 클라우드 제약에 어떻게 대응되는지 정리했다 |

> Phase 02 는 원래 커넥션 풀 자리였으나 TLS 를 먼저 하기로 해서 순서를 바꿨다.
> Phase 01 에서 "앱 단일 스레드 CPU 가 병목"임을 확정했기 때문에, CPU 를 먹는 TLS 를
> 바로 이어서 재는 것이 자연스러웠다.

Phase 순서를 건너뛰지 않는다. 앞 Phase의 측정치가 뒤 Phase의 비교 기준이 되기 때문이다.

---

## 대상 라우트

**단 하나의 라우트만 존재한다.** 라우트를 추가하고 싶어지면 그건 대개 실험 설계가 잘못된 것이다.

```
POST /api/v1/events/{eventId}/reservations
```

읽기(이벤트 정보 + 잔여 수량)와 쓰기(재고 차감 + 예약 생성)를 한 요청 안에 모두 포함해서
커넥션 풀 / 행 락 / 캐시 일관성 / 비동기 분리를 전부 이 라우트 하나로 실험한다.

핵심 파라미터는 **핫키 비율(`HOT_RATIO`)** 이다. 부하가 특정 이벤트 하나에 몰리는 정도를
조절해서 락 경합을 켜고 끌 수 있다. 실험 조건을 적을 때 이 값을 반드시 기록한다.

---

## 스택

확정(Phase 01). 전부 Docker Compose 로 로컬에서 재현 가능해야 한다. 클라우드 의존은 Phase 08 전까지 금지.

| 계층 | 선택 |
|---|---|
| 런타임 | colima (Lima + vz), VM **6 CPU / 8GB**, mount-type sshfs |
| 앱 | Node 24 · Fastify 5 · Drizzle ORM · node-postgres. **단일 프로세스** (Phase 03 부터 3대) |
| DB | Postgres 16-alpine, 설정 무수정 (풀 기본값 10) |
| LB / 프록시 | HAProxy 3.0 (Phase 03~). nginx 는 Phase 02 의 TLS 종료용으로만 사용 |
| 부하 생성 | k6 0.56 |
| 관측 | Prometheus + Grafana + postgres_exporter + `docker stats` 샘플러 + `/proc/net` 캡처 |

Prisma 대신 Drizzle 을 쓴 이유: 커넥션 풀(node-postgres)이 그대로 노출되어
`totalCount/idleCount/waitingCount` 와 획득 대기시간을 직접 계측할 수 있다.

**쓰지 않기로 한 것**: cAdvisor. colima 에서 컨테이너 이름 라벨을 못 붙였고,
대량 시계열로 Prometheus 를 OOM 시켰다 (docs/labs/02-tls.md 참고).

---

## 측정할 때 항상 같이 기록할 것

- k6: RPS, p50/p95/p99, 에러율, `http_req_failed` 사유별 분해, 커넥션 재사용 여부
- 앱: CPU, 메모리, 이벤트루프/스레드풀 지연, 커넥션 풀 대기 시간·대기 큐 길이
- DB: 활성/유휴 커넥션 수, 락 대기, 느린 쿼리, 메모리
- 호스트: Docker 에 할당된 CPU/메모리, **부하 생성기가 병목이 아님을 확인한 근거**

마지막 항목을 빼먹지 않는다. 로컬 부하테스트에서 가장 흔한 거짓 결론이
"서버가 한계"가 아니라 "내 노트북이 한계"인 경우다.
